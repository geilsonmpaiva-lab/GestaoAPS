import {NextResponse} from "next/server";import{z}from"zod";import{commandError}from"@/lib/server/command-response";import{createSupabaseServerClient,isDemoMode,requireActor}from"@/lib/server/supabase";
const schema=z.object({batchId:z.uuid(),countedQuantity:z.number().nonnegative(),notes:z.string().trim().max(2000).default("")});export async function POST(request:Request,context:{params:Promise<{id:string}>}){if(!await requireActor())return NextResponse.json({error:"Não autenticado."},{status:401});const{id}=await context.params;const body=schema.safeParse(await request.json().catch(()=>null));if(!z.uuid().safeParse(id).success||!body.success)return NextResponse.json({error:"Contagem inválida."},{status:422});if(isDemoMode())return NextResponse.json({id:crypto.randomUUID(),difference:0,mode:"demo"});const c=await createSupabaseServerClient();const{data,error}=await c.rpc("record_stock_inventory_count",{p_inventory_id:id,p_batch_id:body.data.batchId,p_counted:body.data.countedQuantity,p_notes:body.data.notes});return error?commandError(error):NextResponse.json(data);}
