import {NextResponse} from "next/server";import{z}from"zod";import{commandError}from"@/lib/server/command-response";import{createSupabaseServerClient,isDemoMode,requireActor}from"@/lib/server/supabase";
const schema=z.object({operationId:z.uuid(),expectedVersion:z.number().int().positive(),applyAdjustments:z.boolean().default(false),reason:z.string().trim().max(2000).default("")}).refine(v=>!v.applyAdjustments||v.reason.length>=3,{message:"Informe o motivo do ajuste.",path:["reason"]});export async function POST(request:Request,context:{params:Promise<{id:string}>}){if(!await requireActor())return NextResponse.json({error:"Não autenticado."},{status:401});const{id}=await context.params;const body=schema.safeParse(await request.json().catch(()=>null));if(!z.uuid().safeParse(id).success||!body.success)return NextResponse.json({error:"Comando inválido."},{status:422});if(isDemoMode())return NextResponse.json({id,status:"COMPLETED",version:body.data.expectedVersion+1,differences:0,adjusted:body.data.applyAdjustments,mode:"demo"});const c=await createSupabaseServerClient();const{data,error}=await c.rpc("complete_stock_inventory",{p_inventory_id:id,p_expected_version:body.data.expectedVersion,p_operation_id:body.data.operationId,p_apply_adjustments:body.data.applyAdjustments,p_reason:body.data.reason});return error?commandError(error):NextResponse.json(data);}
